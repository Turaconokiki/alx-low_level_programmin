#include <stdio.h>
/**
 * main - prints all the digits of base 10.
 *
 * Return: Returns 0, success.
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf("%d", num);
	}
	printf("\n");
	return (0);
}
